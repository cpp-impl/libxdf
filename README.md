# libxdf
libXDF is a simple library for XDF (extensible data format) extensible storage format for time series and associated data

## Build and executable

```sh
#! TODO make a library, instead of executable
cd build/
cmake --build .
./libxdf
```
